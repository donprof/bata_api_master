import Vue from 'vue'
export const Wavesection = Vue.component('wavesection', require('./Wavesection.vue').default)
export const Wavesectionform = Vue.component('wavesectionform', require('./Wavesectionform.vue').default)